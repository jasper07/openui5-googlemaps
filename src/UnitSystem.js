sap.ui.define("openui5/googlemaps/UnitSystem", ["jquery.sap.global"],
    function(jQuery) {
        "use strict";
        // These need to match Google"s constants
        var UnitSystem = {
            IMPERIAL: 1,
            METRIC: 0
        };
        return UnitSystem;
    }, true);
