sap.ui.define(['jquery.sap.global'],
    function(jQuery) {
        "use strict";
        var MapTypeId = {
            ROADMAP: "roadmap",
            SATELLITE: "satellite",
            HYBRID: "hybrid",
            TERRAIN: "terrain"
        };
        return MapTypeId;

    }, /* bExport= */ true);